{
  "name": "project_exam3",
  "version": "1.1.1",
  "description": "project exam front end development",
  "main": "index.js",
  "scripts": {
    "build": "sass src/scss:dist/css",
    "watch": "sass --watch src/scss:dist/css & live-server",
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --w ./**/*.{js,scss,html,md}",
    "lint": "eslint src/**/*.js",
    "lint-fix": "eslint src/**/*.js --cache --fix"
  },
  "keywords": [],
  "author": "saba samuel",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^5.2.2",
    "bootstrap-icons": "^1.10.2"
  },
  "devDependencies": {
    "eslint": "^8.28.0",
    "live-server": "^1.2.2",
    "prettier": "^2.7.1",
    "sass": "^1.56.1"
  }
}
